# Use minimal environment configuration

The boilerplate will use minimal configuration from environment variables with sensible defaults for values such as bind address, database path, and dashboard asset directory. A dedicated config file format is out of scope until user preferences or deployment profiles become real requirements.

**Considered Options**

- Add a config file format from the start.
- Use environment variables with defaults for the initial local control plane.
