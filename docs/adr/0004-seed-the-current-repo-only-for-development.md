# Seed the current repo only for development

Development setup may seed this repository as the first Project so the dashboard has real local data immediately. This seed is explicit development behavior, not production auto-discovery, so a fresh user database starts empty until Projects are added deliberately.

**Considered Options**

- Auto-discover the current repository on first run.
- Explicitly seed the current repository during development setup.
